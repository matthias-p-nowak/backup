log: pybackup.log
db: /tmp/pybackup.db
min_age: 300
max_target_size: 500m
key: topsecret
exclude_flag: ".bkexclude"
email:
  server: localhost
  subject: Result from pybackup
  from: me
  to:
    - me
    - root
backup:
  - /tmp
exclude:
  - "fstab"
  - "\\.git"
  - "bak$"
  - "/tt/"
